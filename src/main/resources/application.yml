spring:
#  profiles:
#    active: local
#  config:
##    import:
##      -optional:file: .env[.properties]
#    activate:
#      on-profile: local


  datasource:
    url: jdbc:mysql://localhost:3306/ticketing_db # ${DATASOURCE_URL}
    username: root # ${DATASOURCE_USER}
    password: dmstjd6918 # ${DATASOURCE_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  logging.level:
    org.hibernate.SQL: debug
    org.hibernate.type: true
