spring:
#  profiles:
#    active: local
#  config:
##    import:
##      -optional:file: .env[.properties]
#    activate:
#      on-profile: local


  datasource:
    url: jdbc:mysql://localhost:3307/ticketing_db # ${DATASOURCE_URL}
    username: user1 # ${DATASOURCE_USER}
    password: 1234 # ${DATASOURCE_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
      ddl-auto: create
    properties:
      hibernate:
#        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  logging.level:
    org.hibernate.SQL: debug
    org.hibernate.type: true
